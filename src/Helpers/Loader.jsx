import { useEffect } from "react";

const Loader = () => {
	useEffect(() => {
		// nProgress.start();

		// return () => {
		//   nProgress.done();
		// };
	}, []);

	return null;
};

export default Loader;
